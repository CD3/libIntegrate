Use this directory for data files that are needed by your unit tests. The `CMakeLists.txt` file in
the `testing` directory will copy all files in this directoy to the working directory for the unit
test executables.
